#include <iostream>
#include <algorithm>
#include <set>
using namespace std;

int t;
int n, d;
int l, r;
int arr[200010];
int cnt,mid;

int tmp_arr[200010];

int main()
{
   scanf("%d", &t);
   while (t--)
   {
      scanf("%d %d", &n, &d);
      for (int i = 1; i <= n; i++)
         scanf("%d", &arr[i]);
      mid =0;
      cnt =0;
      
      
      printf("%d\n", cnt);
   }


}